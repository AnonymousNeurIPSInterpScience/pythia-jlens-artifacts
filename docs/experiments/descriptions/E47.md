# E47 — rank-4 disagreement-subspace ablation across five corpus pairs, absolute-movement metric

## WHAT WAS RUN

Five corpus pairs, fixed in the script as a constant, were run at rank `k = 4` only. For each pair
`(P, Q)`, `Delta_l = J^P_l − J^Q_l` was formed per band layer and its SVD taken; three arms — `top`,
`random` (seeded `torch.Generator().manual_seed(0)`), `bottom` — replaced the operator by
`T_l = J^P_l − B (B^T J^P_l)` and were rescored. Where an arm had already been scored by E46, its
stored `read` values were reused rather than rescored (`source: "reused"`); the two pairs E46 did not
run were scored (`source: "scored"`). Movement was recomputed on an absolute scale,
`move(S) = read(J^P, S) − read(J^P_abl, S)`, and per-set disagreement as
`disagree(S) = read(J^P, S) − read(J^Q, S)`. Per pair and arm, `selectivity_r` is
`r(|disagree(S)|, |move(S)|)` over the five admitted sets. `delta_rel_fro` is the median over the
band of `||Delta_l||_F / ||J^P_l||_F`.

## CONFIGURATION

| field | value |
|---|---|
| model | `EleutherAI/pythia-410m-deduped` — `e47_ablation_pairs.json -> model` |
| checkpoint / revision | NOT RECORDED |
| fitting corpora (operators) | loaded as `results/e28_<corpus>_410m_n400_s0.pt` — `t42_ablation_pairs.py:132–134` |
| pairs | `Github→USPTO_Backgrounds`, `Github→StackExchange`, `Wikipedia_en→USPTO_Backgrounds`, `Wikipedia_en→StackExchange`, `Pile-CC→StackExchange` — `t42_ablation_pairs.py:75–77` (`PAIRS`), and `e47_ablation_pairs.json -> pairs` keys |
| rank | `4` — `e47_ablation_pairs.json -> rank`; script constant `RANK = 4` at `t42_ablation_pairs.py:78` with the inline comment `"E46: at k=32 the random arm already bites, so the clean contrast is k=4"` |
| N | not stored in the results file; the loaded artifacts are the `n400_s0` files |
| seeds | operator seed `s0`; `torch.Generator().manual_seed(0)` for the random arm |
| band | `[9,…,21]` — `e47_ablation_pairs.json -> band` |
| target layer | NOT RECORDED |
| skip_first | NOT RECORDED |
| eval sets used | `multihop`, `multilingual`, `order-ops`, `poetry`, `typo` — `t42_ablation_pairs.py:74` (`SETS`) |
| K grid | `[1, 2, 5, 10, 20, 50, 100]` — `t42_ablation_pairs.py:73` |
| aggregations stored | `read` values are the K-grid-averaged persistence-style score defined in `t42_ablation_pairs.py:122–130` (`>= len(BAND)//2` band layers). No `min` column |
| device | argparse default `cpu` — `t42_ablation_pairs.py:91`. Actual invocation not recorded |
| readout | no `readout_convention` field. `t42_ablation_pairs.py:116` calls `readout_position(tok, name, it["prompt"])` on the item prompt as loaded |
| reuse source | `results/e46_disagreement_ablation.json` — argparse default `--reuse` at `t42_ablation_pairs.py:92` |
| date run | no `provenance` block. File mtime `2026-08-14T14:47:41-0700` |

## PRE-REGISTERED RULE (verbatim)

`e47_ablation_pairs.json -> prereg` stores `"in-file docstring, fixed before running"`.
From the module docstring of `experiments/t42_ablation_pairs.py` (lines 35–51):

> PRE-REGISTRATION — fixed before any number below existed
>   PRIMARY   Per pair, r( |disagree(S)| , |move(S)| ) across the 5 admitted sets, for the top-k arm.
>   RULE      * top-k r >= 0.5 in >= 4 of 5 pairs AND random-k r < 0.3 in >= 4 of 5
>               => ACCEPT: selectivity is invariant across pairs. A law, not a Github fact.
>             * top-k r >= 0.5 ONLY in pairs with Github as an endpoint
>               => REJECT: the effect is a Github artifact and the honest paper is narrower.
>             * top-k and random-k behave alike => REJECT: any rank-k ablation does this.
>   SECONDARY Does magnitude scale? r( ||Delta||_F , max_S |move(S)| ) across the 5 pairs.
>             Selectivity invariant + magnitude scaling = the law statement.
>   CONTROL   random-k (matched rank, random orthonormal output directions) in every pair. This is
>             load-bearing: it separates "this subspace" from "any k directions".
>   CONTROL   bottom-k should be inert in every pair.
>   BIAS      5 concept SETS per pair, not 1310 tokens -- the stored ladder aggregates ranks per set.
>             Token granularity is the stronger test and is deferred, not dismissed.
>   REUSE     Pairs already scored by E46 are read from its stored per-arm `read` values and
>             re-analysed under the absolute metric; only new pairs are scored. No rescoring.

The pair list itself is registered in the same docstring (lines 16–23):

> PAIRS, chosen before running, to span ||Delta||
>   Github -> USPTO_Backgrounds   the extreme pair (the two ends of the measured read spread)
>   Github -> StackExchange       second extreme, Github endpoint
>   Wikipedia_en -> USPTO_Backg.  moderate, no Github
>   Wikipedia_en -> StackExchange moderate English prose, no Github
>   Pile-CC -> StackExchange      EXPECTED WEAK: the one pair whose read asymptotes do NOT separate
>                                 (t = -0.20, SPINE A2.4). If the mechanism is real this pair should
>                                 show little movement, because there is little disagreement to ablate.

Registration source: the docstring of `experiments/t42_ablation_pairs.py`. No file under
`docs/archive/prereg/` names E47. No spec document exists at `docs/experiments/preregs/E47*.md`.

## CONTROLS (as recorded)

| control | required value | observed value | fired | source |
|---|---|---|---|---|
| random-k | `r < 0.3` in `>= 4 of 5` pairs (code gate `abs(v) < 0.3` at `t42_ablation_pairs.py:182`) | `n_pairs_random_lt_0.3 = 0` | **`false`** against the 4-of-5 bar | `e47_ablation_pairs.json -> summary.n_pairs_random_lt_0.3`, `-> summary.selectivity_random` |
| bottom-k | docstring: "bottom-k should be inert in every pair." No numeric bar stated; no `fires` key and no gate in code | stored per pair: `selectivity_bottom` = +0.676186, −0.252713, +0.830333, +0.813655, −0.371711 | no `fires` key | `e47_ablation_pairs.json -> summary.selectivity_bottom` |
| Github-endpoint check | rule branch: "top-k r >= 0.5 ONLY in pairs with Github as an endpoint" | `n_nonGithub_pairs_top_ge_0.5 = "3/3"` | — | `e47_ablation_pairs.json -> summary.n_nonGithub_pairs_top_ge_0.5` |

### Record elsewhere for the bottom-k control

`results/r4e_control_power.json -> decision_rule_verbatim` (quoted from
`docs/experiments/preregs/R4_corrections.md`) contains:

> Include E47's bottom-k control, which was computed, stored, never gated, and would have failed 3 of 5.

The hand-audit table in `tools/r4e_control_power.py:61–67` keys an entry
`("e47_ablation_pairs", "bottom")` with class `COMPUTED_STORED_NEVER_GATED` and the text:

> the bottom-k arm was computed and stored and no decision rule was ever attached to it. Applying the
> SAME criterion the top-k arm is gated by (selectivity >= 0.5) to bottom-k, 3 of 5 pairs clear the
> bar -- so the control, had it been gated, would have failed on 3 of 5. Recomputed here rather than
> asserted.

The string `COMPUTED_STORED_NEVER_GATED` does not appear anywhere in
`results/r4e_control_power.json`, and that file's `all_control_rows` list contains no row for
`results/e47_ablation_pairs.json`.

## NUMBERS (as stored)

### PRIMARY — `selectivity_r` per pair per arm

Source: `e47_ablation_pairs.json -> summary.selectivity_<arm>` (identical values at
`-> pairs.<pair>.arms.<arm>.selectivity_r`).

| pair | `delta_rel_fro` | `top` | `random` | `bottom` |
|---|---|---|---|---|
| Github→USPTO_Backgrounds | 0.7316238295128771 | **+0.6156383234735877** | +0.8683138881706304 | +0.6761858448923304 |
| Github→StackExchange | 0.5310294397534902 | **+0.037886105164915744** | +0.35789987398865764 | −0.2527131013218424 |
| Wikipedia_en→USPTO_Backgrounds | 0.539657 | **+0.9498469042702207** | +0.9271887199762486 | +0.830333233055002 |
| Wikipedia_en→StackExchange | 0.540662 | **+0.8368777257857798** | +0.5328194939641212 | +0.8136550604202794 |
| Pile-CC→StackExchange | 0.422251 | **+0.7558589460641069** | +0.6701019020353106 | −0.37171084011405064 |

`delta_rel_fro` source: `e47_ablation_pairs.json -> pairs.<pair>.delta_rel_fro`.

### Stored summary counters and the secondary

Source: `e47_ablation_pairs.json -> summary`.

| field | value |
|---|---|
| `n_pairs_top_ge_0.5` | `4` |
| `n_pairs_random_lt_0.3` | `0` |
| `n_nonGithub_pairs_top_ge_0.5` | `"3/3"` |
| `r_magnitude_vs_delta` (SECONDARY) | `0.6255854541806675` |

### `disagree(S)` per pair

Source: `e47_ablation_pairs.json -> pairs.<pair>.disagree`.

| pair | multihop | multilingual | order-ops | poetry | typo |
|---|---|---|---|---|---|
| Github→USPTO_Backgrounds | −0.015257 | −0.011965 | +0.009091 | +0.000000 | −0.096726 |
| Github→StackExchange | −0.009709 | −0.010515 | +0.016883 | +0.000000 | −0.087798 |
| Wikipedia_en→USPTO_Backgrounds | +0.001387 | −0.011965 | +0.007792 | −0.002915 | −0.052083 |
| Wikipedia_en→StackExchange | +0.006935 | −0.010515 | +0.015584 | −0.002915 | −0.043155 |
| Pile-CC→StackExchange | +0.002774 | −0.001813 | +0.002597 | −0.002915 | −0.004464 |

### `move(S)`, `top` arm

Source: `e47_ablation_pairs.json -> pairs.<pair>.arms.top.move`; `source` from `...arms.top.source`.

| pair | source | multihop | multilingual | order-ops | poetry | typo |
|---|---|---|---|---|---|---|
| Github→USPTO_Backgrounds | reused | +0.001387 | +0.009790 | +0.019481 | +0.002915 | +0.020833 |
| Github→StackExchange | reused | +0.002774 | +0.004351 | +0.019481 | +0.002915 | +0.005952 |
| Wikipedia_en→USPTO_Backgrounds | reused | −0.004161 | +0.001088 | +0.006494 | +0.001458 | +0.025298 |
| Wikipedia_en→StackExchange | scored | −0.004161 | −0.002538 | +0.012987 | +0.001458 | +0.014881 |
| Pile-CC→StackExchange | scored | −0.001387 | +0.001813 | +0.001299 | +0.001458 | +0.002976 |

### `move(S)`, `random` arm

| pair | source | multihop | multilingual | order-ops | poetry | typo |
|---|---|---|---|---|---|---|
| Github→USPTO_Backgrounds | reused | −0.001387 | −0.000725 | −0.002597 | +0.000000 | −0.004464 |
| Github→StackExchange | reused | +0.001387 | +0.001813 | −0.001299 | +0.000000 | +0.001488 |
| Wikipedia_en→USPTO_Backgrounds | reused | +0.001387 | −0.002175 | −0.001299 | +0.000000 | +0.004464 |
| Wikipedia_en→StackExchange | scored | +0.001387 | +0.000725 | +0.000000 | +0.000000 | +0.001488 |
| Pile-CC→StackExchange | scored | +0.001387 | +0.001813 | +0.000000 | +0.000000 | +0.004464 |

### `move(S)`, `bottom` arm

| pair | source | multihop | multilingual | order-ops | poetry | typo |
|---|---|---|---|---|---|---|
| Github→USPTO_Backgrounds | reused | +0.000000 | +0.000363 | −0.002597 | +0.000000 | −0.002976 |
| Github→StackExchange | reused | −0.001387 | +0.000000 | +0.002597 | +0.000000 | +0.000000 |
| Wikipedia_en→USPTO_Backgrounds | reused | +0.000000 | −0.000363 | −0.001299 | +0.001458 | −0.002976 |
| Wikipedia_en→StackExchange | scored | +0.001387 | −0.000725 | −0.005195 | +0.001458 | −0.005952 |
| Pile-CC→StackExchange | scored | +0.000000 | +0.000725 | −0.002597 | +0.000000 | +0.000000 |

### `baseline_read`

Source: `e47_ablation_pairs.json -> baseline_read.<corpus>.<set>` (identical to
`e46_disagreement_ablation.json -> baseline_read`).

| corpus | multihop | multilingual | order-ops | poetry | typo |
|---|---|---|---|---|---|
| Github | 0.004160887694784573 | 0.028281363126422678 | 0.019480519528899874 | 0.004373177752963134 | 0.08333333488553762 |
| Wikipedia_en | 0.020804438207830702 | 0.028281362461192266 | 0.018181818138275827 | 0.001457725917654378 | 0.12797618830310448 |
| StackExchange | 0.013869625649281911 | 0.03879622795752117 | 0.0025974025151559283 | 0.004373177752963134 | 0.17113095468708447 |
| Pile-CC | 0.016643550779138292 | 0.036983320356479714 | 0.005194805030311857 | 0.001457725917654378 | 0.16666666977107525 |
| USPTO_Backgrounds | 0.019417475908994675 | 0.04024655504950455 | 0.010389610060623713 | 0.004373177752963134 | 0.18005952225731953 |

Per-arm `read` values for all five pairs are stored at
`e47_ablation_pairs.json -> pairs.<pair>.arms.<arm>.read`.

## VERDICT STRING AS STORED

> "REJECT — top-k behaves like random-k; any rank-k ablation does this"

Stored in: `results/e47_ablation_pairs.json -> VERDICT`.

The verdict string is selected in code at `t42_ablation_pairs.py:194–201`: `ACCEPT` if
`n_top >= 4 and n_rnd >= 4`; `REJECT — selectivity only with a Github endpoint` if
`n_top_nogh == 0 and n_top > 0`; `REJECT — top-k behaves like random-k` if `n_top >= 4 and n_rnd < 4`;
else `NULL/MIXED`. Here `n_top = 4`, `n_rnd = 0`, `n_top_nogh = 3`.

## PROVENANCE

| item | value |
|---|---|
| spec | none — no `docs/experiments/preregs/E47*.md` exists |
| prereg | `experiments/t42_ablation_pairs.py` module docstring; `e47_ablation_pairs.json -> prereg = "in-file docstring, fixed before running"` |
| script | `experiments/t42_ablation_pairs.py`, sha256 `8f15eee97651bcac699d92263d77034935c7561d0344b518c3ed2862df61b558` |
| results file | `results/e47_ablation_pairs.json`, sha256 `16aada1aaac0d41f41771462256600ae57ed0f05b1492d7214472671c7abe34c`, 11628 bytes |
| provenance block | **absent.** No `provenance` key; no git commit, argv, env, or input hashes |
| git log for the results file | `1340e76 Restructure the repo into src/ + experiments/; retire docs/ and FUTURE_IMPROVEMENTS` |
| file mtime | `2026-08-14T14:47:41-0700` |
| inputs named in the script (not hashed in the file) | `results/e28_<corpus>_410m_n400_s0.pt`, `results/e46_disagreement_ablation.json` |

## RELATED / SUPERSEDED FILES

- `results/e46_disagreement_ablation.json` — the reuse source; three of E47's five pairs carry
  `"source": "reused"` for all three arms.
- `results/r4_corrections.json -> M-20_random_beats_top` — recomputes from E47's summary:
  `claim_as_written = "random better on one pair"`, `observed_n_pairs = 2`, `of = 5`,
  `which_pairs = ["Github->USPTO_Backgrounds", "Github->StackExchange"]`,
  `all_are_github_pairs = true`, `slate_required = "2 of 5, both Github pairs"`,
  `agrees_with_slate = true`,
  `verdict = "the claim UNDERSTATES: random beats top-k on 2 of 5 pairs, and both are Github"`.
  The `selectivity_top` and `selectivity_random` tables it stores match `e47_ablation_pairs.json -> summary`.
- `results/r4_corrections.json -> DISAGREEMENTS` records:
  `"R4l — five candidates rest on in-file docstrings, not three, and E47 contributes no predictor row at all (see R4l.DISAGREEMENT_WITH_THE_SLATE)"`.
- `results/r4e_control_power.json` — its `decision_rule_verbatim` names E47's bottom-k control and
  the "3 of 5" number; no row for E47 appears in its `all_control_rows`.
- `docs/validity/EXPERIMENT_VALIDITY_AUDIT.md:89` records: "E47's paper numbers exact (**top 0.6156 vs
  random 0.8683** on Github→USPTO — the paper's '+0.87 vs +0.62')."
- `results/e56_predictor_registry.json` — does not list `e47_ablation_pairs.json` among its seven
  inputs.
