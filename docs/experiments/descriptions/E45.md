# E45 — between-corpus operator disagreement resolved per readout direction, tested against per-cell readability

## WHAT WAS RUN

For each of the five stored N=400 seed-0 operators, `Delta_P = J^P − mean_corpora(J)` was formed per
band layer. For each concept token `c` belonging to an eval set, `u_c = Delta^T W[c]` was formed and
`s_c = u_c^T Sigma u_c` computed, with `Sigma = E_{x~eval}[h h^T]` at that layer; `s_c` was averaged
within eval set and taken as a median over the band. Two variants of `W` were used: `W = W_U`
(`pre_norm`) and `W = W_U @ G^T` where `G` is the Jacobian of the model's final norm linearised at
the mean band activation (`post_norm`). The outcome was per `(corpus, eval-set)` readability read
from `results/ladder410/*.json` (`persist`, mean over `N >= 75`, mean over seeds 0–2). Both sides
were z-scored within eval set, and Pearson `r` was computed over the 25 cells with
leave-one-set-out and leave-one-corpus-out.

## CONFIGURATION

| field | value |
|---|---|
| model | `EleutherAI/pythia-410m-deduped` — `e45_disagreement_geometry.json -> model` |
| checkpoint / revision | NOT RECORDED |
| fitting corpora (operators) | `Github`, `Wikipedia_en`, `StackExchange`, `Pile-CC`, `USPTO_Backgrounds`, loaded as `results/e28_<corpus>_410m_n400_s0.pt` — `experiments/t40_disagreement_geometry.py:158` |
| N | not stored in the results file; the loaded artifacts are the `n400_s0` files. The outcome side uses ladder cells with `int(n) >= 75` — `t40_disagreement_geometry.py:194` |
| seeds | operator seed `s0`; outcome averaged over ladder seeds `range(3)` — `t40_disagreement_geometry.py:194`. C2 uses `torch.Generator().manual_seed(0)` |
| band | `[9,…,21]` — `e45_disagreement_geometry.json -> band` |
| target layer | NOT RECORDED |
| skip_first | NOT RECORDED |
| eval sets used | `multihop`, `multilingual`, `order-ops`, `poetry`, `typo`. Concept-token counts stored: `{"multihop": 199, "multilingual": 369, "order-ops": 129, "poetry": 346, "typo": 371}` — `e45_disagreement_geometry.json -> n_concept_tokens_per_set` |
| aggregations stored | the outcome is the ladder's `persist` column (`t40_disagreement_geometry.py:194`). No `min` column is stored in this file |
| device | argparse default `cpu` — `t40_disagreement_geometry.py:99`. Actual invocation not recorded |
| readout | no `readout_convention` field. `t40_disagreement_geometry.py:128` calls `readout_position(tok, name, it["prompt"])` on the item prompt as loaded |
| date run | no `provenance` block. File mtime `2026-08-14T14:05:14-0700` |
| stored note | `"Delta_P = J^P - mean_corpora(J); s_c is the per-concept term of D_read, which t39 summed away"` — `e45_disagreement_geometry.json -> note` |

## PRE-REGISTERED RULE (verbatim)

`e45_disagreement_geometry.json -> prereg` stores `"in-file docstring, fixed before running"`.
From the module docstring of `experiments/t40_disagreement_geometry.py`:

> PRE-REGISTRATION — fixed before any number below existed
>   PRIMARY   r( z(s) , z(|readability|) ) over the 25 (corpus, eval-set) cells, where both sides are
>             z-scored WITHIN eval set so task difficulty is removed from each identically.
>   RULE      * |r| >= 0.5 AND survives leave-one-SET-out AND leave-one-CORPUS-out
>               => ACCEPT: the operators' disagreement geometry IS the interaction.
>             * |r| >= 0.5 but fails a leave-one-out => LEVERAGE-DRIVEN, reported as such.
>             * |r| < 0.5 => NULL. The disagreement subspace does not explain which concepts move.
>   CONTROL   C1 s_c computed with Delta = 0 must be exactly 0.
>   CONTROL   C2 a random-direction Delta of matched Frobenius norm must not predict (|r| < 0.3).
>             If it does, the statistic is picking up something about the eval sets, not the corpora.
>   CONTROL   C3 both pre-norm and post-norm variants reported. A result present only pre-norm is
>             reported as a normalization artifact, not a mechanism.
>   BIAS      The outcome is per (corpus, eval SET), not per concept token, because the stored ladder
>             aggregates ranks per set. So s_c is aggregated to set level before testing -- this is a
>             coarser test than the geometry supports, and a per-token outcome would be stronger.

Registration source: the docstring of `experiments/t40_disagreement_geometry.py` (lines 36–50). No
file under `docs/archive/prereg/` names E45. No spec document exists at `docs/experiments/preregs/E45*.md`.

## CONTROLS (as recorded)

| control | required value | observed value | fired | source |
|---|---|---|---|---|
| C1 `s_c` with `Delta = 0` | exactly `0` (code gate `c1 == 0.0`) | `0.0` | `true` | `e45_disagreement_geometry.json -> control_C1_zero_delta`, `-> control_C1_fires` |
| C2 random matched-norm `Delta` | `|r| < 0.3` | `0.030950976174021295` | `control_C2_fires = true` | `e45_disagreement_geometry.json -> control_C2_random_delta_r`, `-> control_C2_fires` |
| C3 both variants reported | both `pre_norm` and `post_norm` present | both blocks present | no `fires` key | `e45_disagreement_geometry.json -> pre_norm`, `-> post_norm` |

### Records elsewhere for C2 and the post-norm variant

`results/e58_algebra_audit.json -> B_e45_proper_C2` stores:

| field | value |
|---|---|
| `coded_C2_r` | `0.030950976174021295` |
| `coded_C2_defect` | `"one Delta shared across all five corpora, so x has zero within-set variance and the control cannot fail"` |
| `proper_C2_r_five_independent_norm_matched_deltas` | `0.34909509131902045` |
| `threshold` | `0.3` |
| `fires` | `false` |
| `VERDICT` | `"DOES NOT FIRE — a norm-matched random operator predicts as well as the real disagreement geometry; E45's primary is confounded"` |

`results/r4e_control_power.json` stores a row for `results/e45_disagreement_geometry.json`, control
`C2`: `n_instances = 2`, `mechanical_class = "FIRED_TRUE"`, `power = "NONE"`,
`example_path = "control_C2_random_delta_r"`, `hand_audited_class = "SELF_COMPARISON"`,
`why = "recorded in the audit as structurally incapable of failing; rebuilt properly it does NOT fire (r = +0.349 against a 0.3 threshold). The stored version is decoration."`

`results/e58_algebra_audit.json -> A_e45_orientation` stores four orientations of the post-norm
`W` term, each against `threshold = 0.5`:

| orientation | `r` | worst LOO set | worst LOO corpus | `survives` | `crosses_the_0p5_threshold` |
|---|---|---|---|---|---|
| `none` | 0.6169298718606291 | multihop 0.53696139646654 | Github 0.20245342469864708 | false | true |
| `coded_G_transpose` | 0.49614222895095655 | typo 0.38987876875470867 | Github 0.1524035039262419 | false | false |
| `correct_G` | 0.4994154833567744 | typo 0.39499077980783137 | Github 0.16817400762862061 | false | false |
| `symmetrised` | 0.494970172207323 | typo 0.3895896933294183 | Github 0.15312346914458108 | false | false |

with `G_relative_asymmetry_median = 0.013555113226175308`,
`layernorm_gamma_mean = 3.4597463607788086`,
`VERDICT = "IMMATERIAL — the corrected orientation lands on the same side of the pre-registered 0.5 threshold as the coded one; E45's stored VERDICT string stands"`,
and `controls.C3_pre_norm_reproduces_stored_e45 = {"stored_pre_norm_r": 0.6169298718606291, "recomputed": 0.6169298718606291, "abs_diff": 0.0, "fires": true}`.

## NUMBERS (as stored)

### PRIMARY

Source: `e45_disagreement_geometry.json -> <variant>.{r,n_cells,worst_loo_set,worst_loo_corpus,survives}`.

| variant | `r` | `n_cells` | worst LOO by set | worst LOO by corpus | `survives` |
|---|---|---|---|---|---|
| `pre_norm` | **0.6169298718606291** | 25 | multihop 0.53696139646654 | Github 0.2024534246986471 | **false** |
| `post_norm` | **0.49614222895095655** | 25 | typo 0.38987876875470867 | Github 0.1524035039262419 | **false** |

Full leave-one-out tables, `e45_disagreement_geometry.json -> <variant>.loo_by_set` and
`-> <variant>.loo_by_corpus`:

| variant | −multihop | −multilingual | −order-ops | −poetry | −typo |
|---|---|---|---|---|---|
| pre_norm | 0.53696139646654 | 0.6102176309761381 | 0.6438304506744612 | 0.7443380738386328 | 0.5400063308388493 |
| post_norm | 0.49062894769317705 | 0.46145585077221873 | 0.5004320756100146 | 0.6396445587743138 | 0.38987876875470867 |

| variant | −Github | −Wikipedia_en | −StackExchange | −Pile-CC | −USPTO_Backgrounds |
|---|---|---|---|---|---|
| pre_norm | 0.2024534246986471 | 0.7175949997961392 | 0.6568928234545184 | 0.5710293024383641 | 0.6397680950558284 |
| post_norm | 0.1524035039262419 | 0.5857116880646671 | 0.5761449664159827 | 0.3944336398355985 | 0.5574966724513039 |

### `s_by_cell`, pre_norm

Source: `e45_disagreement_geometry.json -> pre_norm.s_by_cell`.

| corpus | multihop | multilingual | order-ops | poetry | typo |
|---|---|---|---|---|---|
| Github | 0.06453294306993484 | 0.05565283074975014 | 0.06409358233213425 | 0.060621537268161774 | 0.05715227872133255 |
| Wikipedia_en | 0.02521834336221218 | 0.024450715631246567 | 0.02195018157362938 | 0.024187589064240456 | 0.021993717178702354 |
| StackExchange | 0.02671734429895878 | 0.029058223590254784 | 0.032539475709199905 | 0.030990630388259888 | 0.030744435265660286 |
| Pile-CC | 0.014427314512431622 | 0.016316192224621773 | 0.012237297371029854 | 0.01584223099052906 | 0.013465631753206253 |
| USPTO_Backgrounds | 0.030059009790420532 | 0.03093351423740387 | 0.02836107648909092 | 0.027069009840488434 | 0.025927294045686722 |

### `s_by_cell`, post_norm

Source: `e45_disagreement_geometry.json -> post_norm.s_by_cell`.

| corpus | multihop | multilingual | order-ops | poetry | typo |
|---|---|---|---|---|---|
| Github | 1.0400294065475464 | 1.0481507778167725 | 1.0506365299224854 | 1.1820619106292725 | 1.1752766370773315 |
| Wikipedia_en | 0.7885950207710266 | 0.6339923739433289 | 0.8369501829147339 | 0.6120684742927551 | 0.6616728901863098 |
| StackExchange | 0.9026137590408325 | 0.7817927002906799 | 0.8961479663848877 | 0.7012752890586853 | 0.7113168835639954 |
| Pile-CC | 0.4335557222366333 | 0.4763605296611786 | 0.3831879198551178 | 0.5129296779632568 | 0.45020848512649536 |
| USPTO_Backgrounds | 0.8309922218322754 | 0.9021438360214233 | 0.776004433631897 | 0.8589208722114563 | 0.85048508644104 |

Aggregations: `min` and `persist` columns are not stored per cell in this file; the outcome side is
the ladder's `persist` column only.

## VERDICT STRING AS STORED

> "NULL — disagreement geometry does not explain which concepts move"

Stored in: `results/e45_disagreement_geometry.json -> VERDICT`.

The verdict string is selected in code at `t40_disagreement_geometry.py:243–248` from
`best = rec.get("post_norm", rec["pre_norm"])`: `ACCEPT` if `best["survives"]`, else
`LEVERAGE-DRIVEN` if `abs(best["r"]) >= 0.5`, else the stored `NULL` string. Here
`post_norm.r = 0.49614222895095655` and `post_norm.survives = false`.

## PROVENANCE

| item | value |
|---|---|
| spec | none — no `docs/experiments/preregs/E45*.md` exists |
| prereg | `experiments/t40_disagreement_geometry.py` module docstring; `e45_disagreement_geometry.json -> prereg = "in-file docstring, fixed before running"` |
| script | `experiments/t40_disagreement_geometry.py`, sha256 `03c66c8b54e320e50a0cf26862922c0fa5e2387ea59230bcf225950b6b32cb5d` |
| results file | `results/e45_disagreement_geometry.json`, sha256 `dfd1a336e310f2f0aaf8b966f0b4ae09395a4833cbd14b4481e516c043be8739`, 4406 bytes |
| provenance block | **absent.** No `provenance` key; no git commit, argv, env, or input hashes |
| git log for the results file | `1340e76 Restructure the repo into src/ + experiments/; retire docs/ and FUTURE_IMPROVEMENTS` |
| file mtime | `2026-08-14T14:05:14-0700` |
| inputs named in the script (not hashed in the file) | `results/e28_<corpus>_410m_n400_s0.pt` ×5, `results/ladder410/*.json` |

## RELATED / SUPERSEDED FILES

- `results/e44_dread_410m.json` — same operators and band; E45's stored `note` describes `s_c` as
  the per-concept term of E44's `D_read`.
- `results/ladder410/*.json` — supplies the outcome variable (`by_N.<n>.<set>.persist`, `n >= 75`,
  seeds 0–2).
- `results/e58_algebra_audit.json` — sections `A_e45_orientation` and `B_e45_proper_C2` recompute the
  post-norm orientation and rebuild C2; numbers quoted above.
  `e58_algebra_audit.json -> controls.C3_pre_norm_reproduces_stored_e45.abs_diff = 0.0`.
- `results/r4e_control_power.json` — classifies E45's C2 as `SELF_COMPARISON`, `power = "NONE"`.
- `results/e56_predictor_registry.json` — lists `e45_disagreement_geometry.json` among its inputs and
  registers two rows, `disagreement geometry s_c (pre_norm)` and `(post_norm)`.
- `docs/context/RESULTS_TAXONOMY.md` §1.16 ("E45's null control could not fire (E58)") and
  `paper/CONTEXT.md:256` both record the C2 finding in prose; the numbers above are the stored ones.
