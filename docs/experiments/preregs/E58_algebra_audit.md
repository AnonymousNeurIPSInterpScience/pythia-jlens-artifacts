# E58 — the algebra and measurement checks reading could not settle

**Run 2026-08-16.**

## WHY
`EXPERIMENT_VALIDITY_AUDIT` v2 §8 listed E45/E46/E47's subspace algebra and the containment
streaming implementation as unaudited. Reading settled E46/E47 — the ablation
`(I − BBᵀ)J^P` with `B` from `svd(J^P − J^Q).U` is correct, and E47's verdict logic executes
correctly. Three things could not be settled by reading.

## OUTPUT
`results/e58_algebra_audit.json`

## DESIGN
`experiments/t58_algebra_audit.py`, 410M, band [9,21], E45's exact pipeline.

## PRIMARY
(A) whether E45's coded transpose changes which side of its pre-registered 0.5 threshold it lands
on; (B) whether E45's C2 fires when made capable of failing; (C) E36's measured diagonal overlap.

## DECISION RULE
(A) same side of 0.5 → the stored verdict stands and the transpose is recorded as immaterial;
crosses → the verdict string is wrong and must be corrected. Either way E45 remains a **failed**
predictor (worst LOO ≈ 0.20 in every orientation), so no paper claim reverses.
(B) proper C2 \|r\| ≥ 0.3 → the original was masking a real problem.

## CONTROLS
| control | required | produced | fires |
|---|---|---|---|
| **C1** s with Δ=0 is exactly 0, every orientation | 0.0 | **0.0**, 4/4 | ✓ |
| **C3** the pre-norm variant reproduces stored E45 (it has no G in it) | ≤1e-9 | **0.00e+00** | ✓ |

## RESULT
**(A) The transpose is real and immaterial — by 0.0006.** `t40:165` computes `WU @ G.T` where the
linearisation requires `WU @ G`; they coincide only if the final LayerNorm's Jacobian is symmetric,
and it is not (γ mean 3.46, sd 0.216; ‖G−Gᵀ‖_F/‖G‖_F median **0.0136**). Recomputed: coded
**+0.4961**, correct **+0.4994**, symmetrised **+0.4950**, against a threshold of **0.5**. Same
side. It must not be described as comfortably below.

**(B) E45's C2 could not fire, and when rebuilt it does not.** As written it shares **one** random Δ
across all five corpora, so its x has zero within-set variance — the script's own comment concedes
this. Rebuilt with **five independent norm-matched Δ**, identical pipeline: **r = +0.3491 against a
0.3 threshold — DOES NOT FIRE**, versus +0.4994 for the real geometry. A norm-matched random
operator gets most of the way to the real one.

**(C) E36's diagonal prefix/fit overlap = 0.1704**, near-identical across the five corpora
(0.1701–0.1713), replacing the repo's "~17%" arithmetic guess.

## VERDICT
E45's conclusion (a failed predictor) is unchanged. Its stored `control_C2_fires: true` at r=0.031
**is not evidence of anything**, and no summary may describe the predictor set as "each with firing
null controls." Recorded in `RESULTS_TAXONOMY.md` §1.16 and in the paper's §negative.

## FLAGGED DELTAS
- E45/E46/E47 carry **no provenance block** at all — they predate `src/provenance.py`. Tier B.
- Separately verified this session, not in the results file: an **independent reimplementation of
  the containment chain** reproduces all 12 k=32 values exactly, with the rebuilt index length
  matching the shard bitmaps to the entry (2,465,010). The exposure axis is sound.
- `t35_containment.py --smoke`'s M1-C1 threshold is fixed at 0.1 while the planted corpus is
  `names[0]` — whichever sorts first. On 2026-08-13 that was Github (0.427, fired); today it is
  PubMed (0.021 = exactly 50/2400, does not fire). The pipeline is correct; the gate is
  miscalibrated by an alphabetical accident.

---

<!-- BEGIN GENERATED PROVENANCE — tools/build_provenance.py -->

## PROVENANCE

Generated by `tools/build_provenance.py`; do not edit by hand. `readout` is the
exposure class from `tools/readout_exposure.py`: **CLEAN** or **IMMUNE** need no
re-score, **EXPOSED** or **INHERITED** were produced at the legacy readout.

| results file | bytes | sha256 (first 16) | produced by | readout |
|---|---:|---|---|---|
| `results/e58_algebra_audit.json` | 9,249 | `27521c3e4f373976` | `t58_algebra_audit.py` | EXPOSED |

**Payload checksums** (content only, provenance block excluded):

* `e58_algebra_audit.json` — `d635e656a85cfa0a3a059b5ad5653478`

<!-- END GENERATED PROVENANCE -->
