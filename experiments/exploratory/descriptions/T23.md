# T23 (E27) — P-ladder: between-corpus operator distance against the within-corpus sampling floor

## WHAT WAS RUN

For each corpus, the corpus was split into two halves and an operator fitted on each half at the
same `n`. Two activation-weighted distances were then computed on eval-prompt activations:
`D_act(J_halfA(c), J_halfB(c))` (within-corpus) and `D_act(J_halfA(c1), J_halfA(c2))`
(between-corpus, same `n`). The primary statistic `R` is the median over band layers of the
between-over-within ratio. Five runs are stored: 410M and 70M each in a raw and a LENGTHMATCHED
arm, plus a two-corpus 70M run.

## CONFIGURATION

| field | value |
|---|---|
| model | `EleutherAI/pythia-410m-deduped`; `EleutherAI/pythia-70m-deduped` |
| checkpoint | final |
| fitting corpus | `[wikitext, pile, code]` (all runs except `t23_pq_ladder_70m_wiki_pile.json`, which uses `[wikitext, pile]`) |
| N | per-half; not stored as an explicit `n` field in these files |
| seeds | `seed` = 0 in all five files |
| band | `[9…21]` (410M); `[2, 3]` (70M) |
| target layer | not stored in these files |
| skip_first | not stored in these files |
| eval sets used | `n_eval_prompts` = 60 in all five files |
| aggregations stored | not applicable; the statistic is `D_act`, with `d_act` and `d_act_norm` per band layer plus a `median` |
| device | not stored; `env.torch` = "2.13.0" (410M raw, 70M raw, 70M wiki_pile) or "2.6.0+cu124" (both LENGTHMATCHED arms) |
| readout | not applicable (operator-space distance on eval activations) |
| status | `-> status` = "EXPLORATORY" in all five files |
| date run | NO DATE FIELD STORED; `runtime_s` 24.5 / 24.6 / 10.8 / 5.0 / 10.5 |

## PRE-REGISTERED RULE (verbatim)

Stored verbatim at `results/t23_pq_ladder_*.json -> decision_rule` (identical in all five files):

> "R = median over band layers of D_act(between)/D_act(within, matched n); >=3.0 variant / [1.5,3.0) weakly variant / <1.5 invariant"

The registering text, quoted from `experiments/t23_pq_ladder.py` §PRE-REGISTRATION ("Fixed before
the first number"):

> **PRIMARY OUTCOME**
> R = median over band layers of D_act(between-corpus) / D_act(within-corpus, matched n).
>
> **DECISION RULE**
> R >= 3.0        -> CORPUS-VARIANT. The bias term dominates the sampling floor; S3's premise
>                    holds and the crossover experiment (E30) is worth its compute.
> 1.5 <= R < 3.0  -> WEAKLY VARIANT. Bias is present but comparable to noise; E30 needs a
>                    larger n to separate them, and that cost must be re-priced before running.
> R < 1.5         -> CORPUS-INVARIANT. Report as a NULL RESULT WITH A MECHANISM: the averaged
>                    Jacobian is robust to the fitting corpus, and sec(iii) below reports WHY
>                    (which spectral component absorbs the corpus change).
>
> **CONTROLS, each with the number it must produce to count as firing**
> C1 SELF-DISTANCE. D_act(J, J) must be exactly 0. Any drift means the estimator is wrong.
> C2 SHUFFLE FLOOR. D_act(J_halfA, J^shuf_halfA) -- the distance to a deliberately broken
>    operator on the SAME corpus. Between-corpus distance should be far BELOW this, else
>    "different corpus" is as damaging as "destroyed operator" and the scale is uninterpretable.
> C3 NORMALISED FORM. D_act / E||J_P h||^2 is reported alongside the raw value, because the raw
>    value tracks activation norm and therefore depth.
>
> **DECLARED BIAS**
> Small n per half. 70m first, where the E18 law says n=92 gives ~5% operator error at eps=5%;
> the halves are therefore ABOVE that error and the within-corpus floor is CONSERVATIVE (too
> large), which biases R DOWNWARD -- i.e. against finding variance. Stated so a null is not
> over-read.
> 70m is also the model where sec F10 shows the unembedding is nearly rank-1; a corpus effect on
> the OPERATOR is still well-defined there, but its behavioural consequence may not be.

Registration source: in-file docstring of `experiments/t23_pq_ladder.py`. The results files record
`prereg = "in-file docstring, fixed before the first number"` at
`-> prereg` = "in-file header, fixed before the first number". No file in
`docs/archive/prereg/` registers T23/E27.

## CONTROLS (as recorded)

| control | required value | observed value | fired | source |
|---|---|---|---|---|
| C1 SELF-DISTANCE | `D_act(J, J)` exactly 0 | `C1_self_distance_max` = 0.0 in all five files | `C1_fires` = true in all five | `-> C1_self_distance_max`, `-> C1_fires` |
| C2 SHUFFLE FLOOR | between-corpus distance "far BELOW" the shuffle floor (no numeric threshold registered) | see table below | no `C2_fires` key in any file | `-> C2_shuffle_floor_median`, `-> C2_between_over_shuffle` |
| C3 NORMALISED FORM | reported alongside the raw value | `d_act_norm` present per band layer for every corpus and pair | reported | `-> within_corpus.<c>.d_act_norm`, `-> between_corpus.<pair>.d_act_norm` |

C2 values — `-> C2_shuffle_floor_median` and `-> C2_between_over_shuffle`:

| file | shuffle floor median | between/shuffle |
|---|---|---|
| `t23_pq_ladder_410m.json` | wikitext 394.62603759765625, pile 359.3499755859375, code 302.6611022949219 | wikitext\|pile 0.16278919928099533, wikitext\|code 0.30299700058451645, pile\|code 0.04913622175589071 |
| `t23_pq_ladder_410m_LENGTHMATCHED.json` | wikitext 415.9534912109375, pile 356.54461669921875, code 305.40826416015625 | 0.12386691318553526, 0.23756999417843486, 0.042824776085578005 |
| `t23_pq_ladder_70m.json` | wikitext 3790.42529296875, pile 2808.5869140625, code 2642.093017578125 | 0.0773357738527943, 0.11301013268049115, 0.018988018044794438 |
| `t23_pq_ladder_70m_LENGTHMATCHED.json` | wikitext 3790.321044921875, pile 2819.725341796875, code 2628.9208984375 | 0.06611201321315074, 0.10484769308491966, 0.01760330290460863 |
| `t23_pq_ladder_70m_wiki_pile.json` | wikitext 3790.42529296875, pile 2808.5869140625 | wikitext\|pile 0.07275128538212715 |

## NUMBERS (as stored)

Primary `R` and per-pair `R` — `-> R`, `-> R_per_pair`:

| file | R | wikitext\|pile | wikitext\|code | pile\|code |
|---|---|---|---|---|
| `t23_pq_ladder_410m.json` | 2.596775770187378 | 2.575831890106201 | 3.570662260055542 | 1.3876919746398926 |
| `t23_pq_ladder_410m_LENGTHMATCHED.json` | 3.459711790084839 | 3.459711790084839 | 5.063662528991699 | 1.3125115633010864 |
| `t23_pq_ladder_70m.json` | 5.349780559539795 | 5.349780559539795 | 7.975103378295898 | 1.4292482137680054 |
| `t23_pq_ladder_70m_LENGTHMATCHED.json` | 3.967928886413574 | 3.967928886413574 | 5.61839485168457 | 1.2303110361099243 |
| `t23_pq_ladder_70m_wiki_pile.json` | 5.349780559539795 | 5.349780559539795 | — | — |

Note: in `t23_pq_ladder_410m.json` the top-level `R` (2.596775770187378) differs from the
`wikitext|pile` pair value (2.575831890106201); in the other four files the top-level `R` equals
the `wikitext|pile` pair value exactly.

Within-corpus medians — `-> within_corpus.<corpus>.median`:

| file | wikitext | pile | code |
|---|---|---|---|
| 410m | 31.29226303100586 | (stored) | (stored) |
| 410m LENGTHMATCHED | 14.201894760131836 | (stored) | (stored) |
| 70m | 41.26363754272461 | 25.234033584594727 | 26.264249801635742 |
| 70m LENGTHMATCHED | 33.18638229370117 | 22.759355545043945 | 29.41654396057129 |
| 70m wiki_pile | 41.26363754272461 | 25.234033584594727 | — |

Between-corpus medians — `-> between_corpus.<pair>.median`:

| file | wikitext\|pile | wikitext\|code | pile\|code |
|---|---|---|---|
| 410m | 49.26995849609375 | (stored) | (stored) |
| 410m LENGTHMATCHED | 37.829978942871094 | (stored) | (stored) |
| 70m | 204.32830810546875 | 298.5832824707031 | 50.16810989379883 |
| 70m LENGTHMATCHED | 173.80325317382812 | 275.63629150390625 | 46.27769088745117 |
| 70m wiki_pile | 204.32830810546875 | — | — |

Per-band-layer `d_act` and `d_act_norm` vectors are stored for every corpus and every pair at
`-> within_corpus.<corpus>.{d_act,d_act_norm}` and `-> between_corpus.<pair>.{d_act,d_act_norm}`.

Derangement layer maps are stored at `-> derangement_layer_map` (22 entries at 410M, 4 at 70M).

## VERDICT STRING AS STORED

| file | VERDICT (verbatim) |
|---|---|
| `results/t23_pq_ladder_410m.json` | "WEAKLY VARIANT — R=2.60 in [1.5,3.0); bias present but comparable to noise, E30 needs a larger n and must be re-priced" |
| `results/t23_pq_ladder_410m_LENGTHMATCHED.json` | "CORPUS-VARIANT — R=3.46 >= 3.0; the distributional bias term dominates the sampling floor, S3's premise holds" |
| `results/t23_pq_ladder_70m.json` | "CORPUS-VARIANT — R=5.35 >= 3.0; the distributional bias term dominates the sampling floor, S3's premise holds" |
| `results/t23_pq_ladder_70m_LENGTHMATCHED.json` | "CORPUS-VARIANT — R=3.97 >= 3.0; the distributional bias term dominates the sampling floor, S3's premise holds" |
| `results/t23_pq_ladder_70m_wiki_pile.json` | "CORPUS-VARIANT — R=5.35 >= 3.0; the distributional bias term dominates the sampling floor, S3's premise holds" |

Stored in: each file's `-> VERDICT` key.

The 410M raw and 410M LENGTHMATCHED files return different branches of the same registered rule
(WEAKLY VARIANT at 2.60 vs CORPUS-VARIANT at 3.46). Both are recorded here without resolution.
`CLAUDE.md` §4 states: *"cite the length-matched column; the raw 410M run self-reports WEAKLY
VARIANT at 2.60."*

## PROVENANCE

| item | value |
|---|---|
| spec | no document in `docs/experiments/preregs/` covers T23/E27 |
| prereg | `experiments/t23_pq_ladder.py` docstring §PRE-REGISTRATION; nothing in `docs/archive/prereg/` |
| script | `experiments/t23_pq_ladder.py` |
| results | `results/t23_pq_ladder_410m.json` sha256 `f8dd8356f214b92a`, added in `1340e76` |
| | `results/t23_pq_ladder_410m_LENGTHMATCHED.json` sha256 `2956d53bb5b10207`, added in `1340e76` |
| | `results/t23_pq_ladder_70m.json` sha256 `c883427cddfff055`, added in `1340e76` |
| | `results/t23_pq_ladder_70m_LENGTHMATCHED.json` sha256 `46102213cb5ebe36`, added in `1340e76` |
| | `results/t23_pq_ladder_70m_wiki_pile.json` sha256 `2b8b9467090688c8`, added in `1340e76` |
| payload_sha256 | absent on all five (no `provenance` block) |
| argv | stored per file at `-> argv` |

## RELATED / SUPERSEDED FILES

- `results/t23_pq_ladder_70m_wiki_pile.json` — the two-corpus subset of `t23_pq_ladder_70m.json`; its `R` and both `median` values are identical to that file's `wikitext|pile` entries.
- `results/e34a_dact_vs_floor_410m.json`, `results/e34b_jeval_dact_410m.json` — the same `D_act` instrument (`src/metrics_shift.m4_action_divergence`) applied to the corpus-floor question.
- `results/r7_matched_pools_410m.json` (R7) — the fitting-side length/lexical matching arm.
- `results/e48c_exposure_vs_read.json` — the other shift-axis measurement.
