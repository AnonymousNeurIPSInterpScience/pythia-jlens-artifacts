# T15B — generic / layer-specific partition recomputed under three aggregations

## WHAT WAS RUN
No new compute. The per-layer AUCs already stored by T15 were re-partitioned into a "generic"
component and a "layer-specific" component under three aggregations — best single layer, mean over
layers, and min over layers — for the two eval sets T15 covers (`multihop`, `typo`). Each of the six
(set, aggregation) cells was assigned a verdict string by the producing code.

## CONFIGURATION
| field | value |
|---|---|
| model | NOT RECORDED in this file. The file's `status` reads `"EXPLORATORY — reanalysis of t15, no new compute"`; T15's model is `EleutherAI/pythia-410m-deduped` (`results/t15_shuffle_diagnostic_410m.json -> model`) |
| checkpoint / lens | NOT RECORDED in this file; T15's lens is `pythia/results/lenses/ladder/lens_410m_n200_db128.pt` (`results/t15_shuffle_diagnostic_410m.json -> lens`) |
| fitting corpus | NOT RECORDED in this file |
| N | NOT RECORDED in this file |
| seeds | NOT RECORDED in this file |
| band | NOT RECORDED in this file |
| target layer | NOT RECORDED in this file |
| skip_first | NOT RECORDED in this file |
| eval sets used | `multihop`, `typo` — `results/t15b_partition_aggregation_dependence.json -> per_cell` |
| aggregations stored | `best single layer`, `mean over layers`, `MIN over layers`. `persist` is NOT stored. — `results/t15b_partition_aggregation_dependence.json -> per_cell` |
| device | NOT RECORDED |
| readout | NOT RECORDED in this file; the upstream T15 scoring path strips (`experiments/t15_shuffle_diagnostic.py:89`) |
| date run | file mtime 2026-08-11 13:24 |
| runtime | NOT RECORDED (no `runtime_s` key) |

## PRE-REGISTERED RULE (verbatim)
> NOT PRE-REGISTERED — the file stores no `decision_rule` key, and no threshold appears anywhere in it.

Registration source: NONE LOCATABLE. No `T15b` document exists in `docs/experiments/preregs/` and no file in
`docs/archive/prereg/` covers it.

## CONTROLS (as recorded)
| control | required value | observed value | fired | source |
|---|---|---|---|---|
| — | — | — | — | NOT RECORDED — the file stores no controls section and no control keys |

## NUMBERS (as stored)
`results/t15b_partition_aggregation_dependence.json -> per_cell`. Each cell stores a `generic`
component, a `specific` component, and a `verdict` string.

| set | aggregation | generic | specific | cell `verdict` (verbatim) |
|---|---|---|---|---|
| multihop | best single layer | −0.0131 | 0.0301 | "LAYER-SPECIFIC" |
| multihop | mean over layers | −0.0216 | 0.0296 | "LAYER-SPECIFIC" |
| multihop | MIN over layers | −0.0048 | 0.0656 | "LAYER-SPECIFIC" |
| typo | best single layer | 0.0202 | 0.0228 | "LAYER-SPECIFIC" |
| typo | mean over layers | 0.0179 | 0.0293 | "LAYER-SPECIFIC" |
| typo | MIN over layers | 0.1244 | −0.0037 | "generic only" |

No `min` / `persist` read-AUC columns are stored in this file; it stores only the decomposition
components above.

## VERDICT STRING AS STORED
The file stores no `VERDICT` key. It stores two free-text fields:

> "finding": "YES. The clean bimodal partition reported in t13b exists ONLY under min-over-layers. Under best-single-layer and mean-over-layers, typo's layer-specific component is POSITIVE (+0.0228, +0.0293) rather than negative (-0.0037), and BOTH sets read as layer-specific. Since t15 independently showed min-over-layers launders the J-vs-shuffled difference, the partition computed under that same metric cannot be trusted."

> "consequence": "The t13b decomposition must be recomputed under a non-existential aggregation before any claim rests on it. The three 'generic-only' sets (poetry, typo, association) are NOT established as generic-only."

Stored in: `results/t15b_partition_aggregation_dependence.json -> finding` and `-> consequence`

The `status` field reads:

> "EXPLORATORY — reanalysis of t15, no new compute"

Stored in: `results/t15b_partition_aggregation_dependence.json -> status`

## PROVENANCE
| item | value |
|---|---|
| spec in `docs/experiments/preregs/` | NONE |
| pre-registration | NONE LOCATABLE |
| script | NOT LOCATABLE in the working tree. There is no `experiments/t15b_*.py`; `ls experiments/` shows `t15_shuffle_diagnostic.py` only. `docs/context/RESULTS_TAXONOMY.md` Appendix A.2 lists this file under "No provenance block — recomputable, but not self-describing" (`has verdict = no`) |
| results file | `results/t15b_partition_aggregation_dependence.json` |
| results sha256 | `3c2d5c5819de5ba3191ecce5f61a071ea8bf1681480c1bdd2104271e62c70ed3` |
| git commit | not determinable per-file: `git log` for this path returns only `1340e76` (2026-08-15) |

## RELATED / SUPERSEDED FILES
- `results/t15_shuffle_diagnostic_410m.json` — the input; T15B's own header states it is a reanalysis
  of it with no new compute.
- `results/t13b_bootstrap_controls_410m.json`, `..._160m.json`, `..._70m.json` — indexed in
  `docs/context/RESULTS_TAXONOMY.md` Appendix A.2 as "T13b — paired item bootstrap on the generic /
  layer-specific dec…"; T15B's `finding` names `t13b` as the decomposition it recomputes.
- `results/t17_reaggregate_410m.json` and `results/t17_reaggregate_160m.json` — store
  `layer_specific` and `generic` components for all six eval sets under four aggregations including
  `persist`; T15B stores two sets under three aggregations.
- `RESEARCH_NOTES.tex` line 2026 indexes `t15`/`t15b` together as backlog item **E15**.
